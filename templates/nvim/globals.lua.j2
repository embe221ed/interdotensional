--------------------------------------------------------------------------------
-- THIS FILE WAS AUTO-GENERATED BY INTERDOTENSIONAL.
-- DO NOT EDIT THIS FILE MANUALLY.
-- Any manual changes will be overwritten during the next generation.
-- Please update the source YAML configuration and templates instead.
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- global custom options
--------------------------------------------------------------------------------{% set nvim_theme = theme.tools.nvim %}
vim.g.vimtex_view_method = "skim"
vim.g.symbol_font = {{ font.symbol_font | lower }}
vim.g.border = "{{ ui.border }}"
vim.g.winblend = {{ nvim_theme.winblend }}

vim.g.indentLine_fileTypeExclude = {
  "lspinfo",
  "packer",
  "checkhealth",
  "dashboard",
  "help",
  "man",
  "diff",
  "git",
  "",
}

vim.g.symbols = {
{% for symbol in nvim.symbols %}  {{ symbol }} = "{{ nvim.symbols[symbol] }}",
{% endfor %}}

vim.g.colorscheme = {
  name = "{{ nvim_theme.colorscheme }}",
  theme = "{{ nvim_theme.theme }}",
  vanilla = {{ nvim_theme.vanilla | lower }},
  plugin = function()
    local _cfg = {{ nvim_theme.plugin }}    _cfg.config = function() require('ui.colorscheme') end
    return _cfg
  end
}

vim.g.lsp = {
  doc = {
    border = {{ nvim.lsp.doc.border | lower }}
  },
  diagnostic = {
    border = "{{ nvim.lsp.diagnostic.border }}"
  }
}

vim.g.goto_preview = {
  border = {
  {%- for symbol in nvim.goto_preview.border %}
    "{{ symbol }}"{{ "," if not loop.last else "" }}
  {%- endfor %}
  },
  title_length = {{ nvim.goto_preview.title_length }},
}

vim.g.statusline = {
  laststatus = {{ nvim.statusline.laststatus }},
}
