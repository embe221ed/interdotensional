################################################################################
# THIS FILE WAS AUTO-GENERATED BY INTERDOTENSIONAL.
# DO NOT EDIT THIS FILE MANUALLY.
# Any manual changes will be overwritten during the next generation.
# Please update the source YAML configuration and templates instead.
################################################################################
# tmux-window-name
set -g @tmux_window_name_use_tilde "True"
set -g @tmux_window_name_show_program_args "False"
run-shell /opt/tmux-window-name/tmux_window_name.tmux

set -g mouse on
{% set tmux_theme = theme.tools.tmux %}
set-option -g pane-active-border-style "fg={{ theme.colors.pink }},{{ tmux.pane.border.style }}"
set-option -g pane-border-style "fg={{ tmux_theme.colors.dim }},{{ tmux.pane.border.style }}"
set-option -g popup-border-style "fg={{ theme.colors.pink }},{{ tmux.popup.border.style }}"
set-option -g clock-mode-colour colour{{ tmux_theme.clock }}

set-option -g pane-border-lines {{ tmux.pane.border.lines }}
set-option -g popup-border-lines {{ ui.border }}
set-option -g pane-border-format "{{ tmux.pane.border.format }}"
set-option -g pane-border-status top

set-window-option -g mode-keys vi
set-option -sg escape-time 10
set-option -g default-terminal "xterm-kitty"
set-option -ga terminal-overrides ",xterm-kitty:Tc"

# unbind keys
unbind Up
unbind Down
unbind Left
unbind Right
unbind '"'
unbind %

# Splitting terminals using | and -
bind - split-window -v -c '#{pane_current_path}'
bind | split-window -h -c '#{pane_current_path}'

# vim-like pane switching
bind -r k select-pane -U 
bind -r j select-pane -D 
bind -r h select-pane -L 
bind -r l select-pane -R 

# vim-like pane resizing
bind -r C-k resize-pane -U 2
bind -r C-j resize-pane -D 2
bind -r C-h resize-pane -L 2
bind -r C-l resize-pane -R 2

# tmux-powerline
set-option -g status on
set-option -g status-interval 2
set-option -g status-left-length 80
set-option -g status-right-length 80

# popup!
bind-key -n M-p if-shell -F '#{==:#{session_name},scratch}' {
  detach-client
} {
  display-popup -w70% -h70% -E "tmux new-session -A -s scratch -c ~"
}

# TPM
## List of plugins
set-option -g @plugin 'tmux-plugins/tpm'
set-option -g @plugin 'erikw/tmux-powerline'

## Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
