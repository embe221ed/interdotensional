################################################################################
# THIS FILE WAS AUTO-GENERATED BY INTERDOTENSIONAL.
# DO NOT EDIT THIS FILE MANUALLY.
# Any manual changes will be overwritten during the next generation.
# Please update the source YAML configuration and templates instead.
#
# leaf (markdown reader) theme: {{ theme.name }}
# Point theme = "..." in ~/.config/leaf/config.toml at this file.
################################################################################

{% set leaf = theme.tools.leaf -%}
{% set c = leaf.colors -%}
{#- Pastel accents that work as surfaces can be too light to carry text on
    light-polarity chips. An optional per-role tools.leaf.text_darken map
    (HLS lightness points) derives readable text variants; surfaces keep the
    raw brand colors. Missing map/keys mean 0 = no-op. -#}
{% set td = leaf.text_darken | default({}) -%}
{% set accent_fg = c.accent | darken(td.accent | default(0)) -%}
{% set accent2_fg = c.accent2 | darken(td.accent2 | default(0)) -%}
{% set warning_fg = c.warning | darken(td.warning | default(0)) -%}
{% set strong_fg = c.fg_strong | darken(td.strong | default(0)) -%}
{% set success_fg = c.success | darken(td.success | default(0)) -%}
base = "{{ leaf.base }}"
syntax = "{{ leaf.syntax }}"

[ui]
toc_bg = "{{ c.bg_deep }}"
toc_border = "{{ c.border }}"
content_bg = "{{ c.bg }}"
scrollbar_hover = "{{ c.accent }}"
status_bg = "{{ c.bg_deep }}"
status_separator = "{{ c.dim }}"
status_brand_fg = "{{ c.bg }}"
status_brand_bg = "{{ accent_fg }}"
status_filename_fg = "{{ strong_fg }}"
status_filename_bg = "{{ c.panel }}"
status_watch_fg = "{{ success_fg }}"
status_watch_bg = "{{ c.success | mix(c.bg_deep, 85) }}"
status_reloaded_fg = "{{ c.bg }}"
status_reloaded_bg = "{{ c.success }}"
status_search_fg = "{{ accent_fg }}"
status_search_bg = "{{ c.panel }}"
status_success_fg = "{{ success_fg }}"
status_success_bg = "{{ c.success | mix(c.bg_deep, 85) }}"
status_warning_fg = "{{ warning_fg }}"
status_error_fg = "{{ c.danger }}"
status_error_bg = "{{ c.danger | mix(c.bg_deep, 85) }}"
status_shortcut_fg = "{{ c.dim }}"
status_percent_fg = "{{ accent_fg }}"
toc_header_fg = "{{ c.dim }}"
toc_active_bg = "{{ c.panel }}"
toc_inactive_bg = "{{ c.bg_deep }}"
toc_accent = "{{ accent2_fg }}"
toc_index_inactive = "{{ c.faint }}"
toc_primary_active = "{{ strong_fg }}"
toc_primary_inactive = "{{ c.muted }}"
toc_secondary_inactive = "{{ c.faint }}"
toc_secondary_text_active = "{{ c.fg }}"
toc_secondary_text_inactive = "{{ c.dim }}"

[markdown]
search_highlight_bg = "{{ c.faint }}"
search_match_bg = "{{ accent_fg }}"
code_gutter = "{{ c.faint }}"
blockquote_marker = "{{ c.dim }}"
list_level_1 = "{{ success_fg }}"
list_level_2 = "{{ c.info }}"
list_level_3 = "{{ c.special }}"
ordered_list = "{{ accent_fg }}"
table_border = "{{ c.faint }}"
table_separator = "{{ c.border }}"
table_header = "{{ accent_fg }}"
table_cell = "{{ c.fg }}"
heading_1 = "{{ accent_fg }}"
heading_2 = "{{ success_fg }}"
heading_3 = "{{ c.info }}"
heading_4 = "{{ c.special }}"
heading_other = "{{ accent2_fg }}"
heading_underline = "{{ c.border }}"
code_frame = "{{ c.faint }}"
code_label = "{{ c.dim }}"
inline_code_fg = "{{ accent2_fg }}"
inline_code_bg = "{{ c.panel }}"
rule = "{{ c.border }}"
link_icon = "{{ c.info }}"
link_text = "{{ c.info }}"
link_hover = "{{ c.info | mix(strong_fg, 40) }}"
blockquote_text = "{{ c.fg }}"
text = "{{ c.fg }}"
strong_text = "{{ strong_fg }}"
latex_inline_fg = "{{ c.special }}"
latex_inline_bg = "{{ c.panel }}"
latex_block_fg = "{{ c.special }}"
mermaid_keyword = "{{ accent2_fg }}"
mermaid_arrow = "{{ c.info }}"
mermaid_label = "{{ success_fg }}"
mermaid_block_fg = "{{ c.fg }}"
mark_fg = "{{ strong_fg }}"
mark_bg = "{{ c.accent | mix(c.bg, 60) }}"
task_checked = "{{ success_fg }}"
task_unchecked = "{{ c.dim }}"
alert_note = "{{ c.info }}"
alert_tip = "{{ success_fg }}"
alert_important = "{{ c.special }}"
alert_warning = "{{ warning_fg }}"
alert_caution = "{{ c.danger }}"
